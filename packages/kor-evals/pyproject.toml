[project]
name = "kor-evals"
version = "0.1.0"
description = "Evaluation framework for KOR agents"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kor-core",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
